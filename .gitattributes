# The vendored tynn-cli toolkit is bash. It MUST keep LF line endings — CRLF
# breaks the shebang line and `\r`-terminated commands once the scripts are
# unpacked from the app bundle on a user's machine (and on the CI build agent).
resources/tynn-cli/bin/** text eol=lf
resources/tynn-cli/lib/** text eol=lf
resources/tynn-cli/install.sh text eol=lf
resources/tynn-cli/*.example text eol=lf
resources/tynn-cli/BUNDLED.txt text eol=lf
