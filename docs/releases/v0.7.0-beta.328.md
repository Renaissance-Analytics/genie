## What’s new

- **Codex agents start again.** Genie was handing codex its App Server binding after the `--` that ends option parsing, so the TUI exited before the agent ever ran.
- **Agents are told how THIS machine writes paths** — Windows, macOS or Linux, whichever Genie is installed on — instead of choosing a convention and handing you one you cannot paste.

## Migration

- Nothing to do.
