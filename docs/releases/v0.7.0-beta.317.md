## What’s new

- **One admin approval when you enable a site for the browser**, not two. The hosts entry and the certificate install now go up together.
- **The file preview beside a question scrolls, wraps, and renders markdown** instead of showing a clipped code buffer. A Source/Rendered switch is there when you want the raw file.
- **The shutdown notice an upgrade sends agents now says SHOWSTOPPER** — it was telling them it could wait, while the upgrade waited for them.

## Migration

- Nothing to do.
